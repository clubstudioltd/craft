{%- embed '_ui/component' with {
    heading: 'Text Input',
} -%}
    {%- block markup -%}
        {%- verbatim -%}

{% include '_includes/atoms/form-field' with {
    type: 'text',
    id: 'text-input',
    label: 'Text Input',
    input: {
        value: 'test',
        classes: ['p-0 text-xl text-red-500'],
        attributes: {
            placeholder: 'Placeholder...',
        }
    },
} only %}

        {%- endverbatim -%}
    {%- endblock -%}
{%- endembed -%}
